import { GetServerSideProps } from "next";
import React from "react";

/**
 * An Publications React Component.
 * @author Jannik Will
 * @version 0.1
 */
const Publications: React.FC = () => {
  return <div></div>;
};

export const getServerSideProps: GetServerSideProps = async () => {
  return { props: {} };
};

export default Publications;
